# --- Secrets ---
.env
.env.*
!.env.example

# --- instance 固有設定 (clone 後 cp instance.config.example.yaml instance.config.yaml で作成) ---
/instance.config.yaml

# --- corpus-raw: 機微情報を含む元ファイル (commit 不可) ---
/corpus-raw/

# --- node / bun ---
node_modules/

# --- OS / Editor ---
.DS_Store
*.swp
.idea/
.vscode/

# --- Cloudflare / Wrangler ---
.wrangler/
.dev.vars

# --- Worker per-instance config (cp wrangler.toml.example wrangler.toml) ---
/worker/wrangler.toml
